const CLIENT = 'https://recruit-mono.onrender.com';

const ROUTES = {
    searchJob: '/search',
    singleJob: '/job',
    candidate: '/candidate-listing',
};

module.exports = {
    CLIENT,
    ROUTES
};